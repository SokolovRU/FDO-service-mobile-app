import React from "react";
import { AuthorizationComp } from "../components/authorization/AuthorizationComp";

export const LoginScreen = ({ navigation }: any) => {
  return <AuthorizationComp />;
};
